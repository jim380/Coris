// #TODO fix issue with websockets proxy
const NETWORK = `regen`

export const nodeRpc1 = `https://coris.network/${NETWORK}/rpc1`;
export const nodeRpc2 = `https://coris.network/${NETWORK}/rpc2`;
export const nodeWs = `wss://coris.network/${NETWORK}/websocket`;
export const appApi = `https://coris.network/${NETWORK}/api`;
export const faucetRpc = 'https://regen.coris.network/api'